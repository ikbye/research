# 核心创新点：时延感知动态图构建算法 (Delay-Aware Dynamic Graph Construction, DADGC)

## 一、问题本质：为什么需要"时延"？

### 1.1 物理事实

大气污染物传播是一个**物理输运过程**：

```
站点A (排放源) ──── 风场携带 ────→ 站点B (受体)
     t₀时刻                         t₀ + τ 时刻
                                    τ = distance / wind_speed
```

- 站点B观测到的PM2.5浓度升高，**不是当前时刻**站点A的影响
- 而是τ小时**之前**站点A的排放，经过物理输运到达站点B
- τ随气象条件变化：风速大→τ小，风速小→τ大；顺风→τ短，逆风→几乎无传播

### 1.2 标准STGNN的盲区

| 维度 | 标准STGNN | 物理现实 |
|------|----------|----------|
| 空间聚合 | 同一时刻聚合邻居信息 | 污染传播有物理时延，t时刻站点B受的是t-τ时刻站点A的影响 |
| 图结构 | 静态或隐式学习 | 传播路径随时延/风向动态变化 |
| 时间建模 | 单节点时间序列建模 | 跨节点-跨时间的联合依赖 |

**核心矛盾**：标准STGNN的消息传递是"即时"的（同时间步），但污染传播是"延迟"的（跨时间步）。

### 1.3 核心思路

**将时延从"隐式参数拟合"提升为"显式图结构属性"**：

- 边不仅有权重 $w_{ij}$（连接强度），还有时延 $\tau_{ij}$（传播时间）
- 消息传递不是"邻居当前状态→当前节点"，而是"邻居过去状态→当前节点"
- 时延受气象条件调制，图结构随时间动态演化

---

## 二、算法总体框架

```
输入: 多站点历史观测序列 X ∈ R^{N×T×F}  (N站点, T时间步, F特征)
      气象特征序列 W ∈ R^{T×M}           (M维气象特征)

输出: 动态时延图序列 {G₁, G₂, ..., G_T}
      其中 G_t = (V, E_t), E_t 中每条边带有时延属性 τ_{ij}^{(t)}

步骤:
  Step 1: 时延发现 — 互相关时延估计
  Step 2: 因果筛选 — Granger因果检验过滤伪时延
  Step 3: 气象调制 — 风场驱动的时延与边权动态演化
  Step 4: 图正则化 — 稀疏性与物理一致性约束
```

---

## 三、Step 1：互相关时延估计

### 3.1 目标

对每对站点 $(i, j)$，估计信号从 $i$ 传播到 $j$ 所需的时间延迟 $\tau_{ij}$。

### 3.2 方法：归一化互相关函数 (NCCF)

对站点 $i$ 和站点 $j$ 的历史观测序列 $x_i, x_j \in \mathbb{R}^T$：

$$\rho_{ij}(\tau) = \frac{\sum_{t=\tau+1}^{T} (x_i^{(t-\tau)} - \bar{x}_i)(x_j^{(t)} - \bar{x}_j)}{\sqrt{\sum_{t=1}^{T}(x_i^{(t)}-\bar{x}_i)^2} \cdot \sqrt{\sum_{t=1}^{T}(x_j^{(t)}-\bar{x}_j)^2}}$$

**物理含义**：$\rho_{ij}(\tau)$ 衡量"站点 $i$ 的 $\tau$ 步前观测与站点 $j$ 的当前观测之间的线性相关程度"。

### 3.3 时延估计

$$\hat{\tau}_{ij} = \arg\max_{\tau \in [0, \tau_{max}]} \rho_{ij}(\tau)$$

其中 $\tau_{max}$ 为物理上合理的最大传播时延（如6小时）。

### 3.4 关键设计：单向时延

注意 $\tau_{ij} \neq \tau_{ji}$，因为：
- $\tau_{ij}$：信号从 $i$ 传到 $j$ 的时延（$i$ 先发生，$j$ 后响应）
- $\tau_{ji}$：信号从 $j$ 传到 $i$ 的时延

**这直接产生了有向图**：如果 $\tau_{ij} = 2$ 且 $\tau_{ji} = \infty$（无显著相关），说明 $i$ 是上游源，$j$ 是下游受体。

### 3.5 输出

- 时延矩阵 $\mathbf{T} \in \mathbb{R}^{N \times N}$，$T_{ij} = \hat{\tau}_{ij}$
- 互相关强度矩阵 $\mathbf{R} \in \mathbb{R}^{N \times N}$，$R_{ij} = \rho_{ij}(\hat{\tau}_{ij})$

---

## 四、Step 2：Granger因果筛选

### 4.1 问题

互相关只能发现"统计关联"，不能区分因果方向。例如：
- 站点A和B都受同一远方源C影响 → A和B高度相关，但A→B无因果关系
- 需要排除这种"伪时延"

### 4.2 方法：条件Granger因果检验

对于站点对 $(i, j)$，检验"$i$ 的过去是否提供了 $j$ 的当前信息**超出**所有其他站点已提供的信息"：

**零假设** $H_0$：$i$ 不是 $j$ 的Granger原因

受限模型（不含 $i$ 的过去）：
$$x_j^{(t)} = \alpha_0 + \sum_{k \in \mathcal{N}(j) \setminus \{i\}} \sum_{l=1}^{p} \alpha_{kl} x_k^{(t-l)} + \epsilon_R$$

完整模型（含 $i$ 的过去）：
$$x_j^{(t)} = \beta_0 + \sum_{k \in \mathcal{N}(j)} \sum_{l=1}^{p} \beta_{kl} x_k^{(t-l)} + \epsilon_U$$

F统计量：
$$F = \frac{(RSS_R - RSS_U) / p}{RSS_U / (T - 2p - 1)}$$

若 $F > F_{crit}$，则拒绝 $H_0$，确认 $i \rightarrow j$ 的Granger因果关系。

### 4.3 与时延的结合

只在互相关显著的边（$R_{ij} > \theta_{corr}$）上做Granger检验，**大幅减少计算量**：

$$\text{因果边集} = \{(i,j) : R_{ij} > \theta_{corr} \text{ 且 } i \xrightarrow{\text{Granger}} j\}$$

### 4.4 输出

- 因果邻接矩阵 $\mathbf{A}^{causal} \in \{0,1\}^{N \times N}$
- 因果时延矩阵 $\mathbf{T}^{causal}$（仅保留因果边的时延值）

---

## 五、Step 3：气象调制的动态图演化

**这是算法的核心创新——让图"动"起来。**

### 5.1 物理动机

时延和边权不是固定的，它们受气象条件（尤其是风）调制：
- **风向**：顺风方向传播加强，逆风方向传播减弱
- **风速**：风速大→时延短、传播强；风速小→时延长、传播弱
- **大气稳定度**：影响垂直混合，间接影响水平传播

### 5.2 风向对齐因子

定义站点 $i$ 到站点 $j$ 的**风向对齐因子**：

$$\phi_{ij}^{(t)} = \cos(\theta_{wind}^{(t)} - \theta_{ij})$$

其中：
- $\theta_{wind}^{(t)}$：$t$ 时刻的风向角
- $\theta_{ij}$：从站点 $i$ 指向站点 $j$ 的方位角

**物理含义**：
- $\phi_{ij} = 1$：风正好从 $i$ 吹向 $j$（完美顺风）→ 强传播
- $\phi_{ij} = 0$：风垂直于 $i \rightarrow j$ 方向 → 弱传播
- $\phi_{ij} = -1$：风从 $j$ 吹向 $i$（逆风）→ 几乎无传播

### 5.3 动态时延计算

$$\tau_{ij}^{(t)} = \frac{d_{ij}}{v_{wind}^{(t)} \cdot \max(\phi_{ij}^{(t)}, 0) + \epsilon}$$

其中：
- $d_{ij}$：站点 $i$ 到 $j$ 的距离
- $v_{wind}^{(t)}$：$t$ 时刻风速
- $\epsilon$：防止除零的小常数

**物理含义**：时延 = 距离 / 有效传播速度
- 顺风时：有效速度 = 风速，时延短
- 逆风时：$\phi_{ij} < 0$，max后为0，时延趋向无穷大 → 边断裂
- 无风时：有效速度 = ε，时延很大 → 边权极弱

### 5.4 动态边权计算

$$A_{ij}^{(t)} = \underbrace{R_{ij}^{causal}}_{\text{因果强度基础}} \cdot \underbrace{\sigma(\phi_{ij}^{(t)})}_{\text{风向调制}} \cdot \underbrace{\sigma\left(\frac{v_{wind}^{(t)}}{v_0}\right)}_{\text{风速调制}} \cdot \underbrace{\exp\left(-\frac{\tau_{ij}^{(t)}}{\tau_0}\right)}_{\text{时延衰减}}$$

其中：
- $R_{ij}^{causal}$：Step 2得到的因果相关强度
- $\sigma(\cdot)$：sigmoid函数，将调制因子映射到 $(0,1)$
- $v_0, \tau_0$：可学习的缩放参数

**四因子解耦**：
1. **因果强度**：静态基础，由数据驱动发现
2. **风向调制**：动态方向性，顺风增强、逆风抑制
3. **风速调制**：动态强度，风速大→传播强
4. **时延衰减**：物理约束，时延越长→影响越弱

### 5.5 动态图的最终形式

在每个时间步 $t$，动态时延图定义为：

$$G_t = (\mathcal{V}, \mathcal{E}_t, \mathbf{A}^{(t)}, \boldsymbol{\tau}^{(t)})$$

其中：
- $\mathcal{V}$：站点节点集（固定）
- $\mathcal{E}_t$：时间步 $t$ 的边集（$A_{ij}^{(t)} > \theta_{edge}$ 的边存在）
- $\mathbf{A}^{(t)} \in \mathbb{R}^{N \times N}$：动态加权邻接矩阵
- $\boldsymbol{\tau}^{(t)} \in \mathbb{R}^{N \times N}$：动态时延矩阵

---

## 六、Step 4：图正则化与物理约束

### 6.1 稀疏性约束

动态图应该保持稀疏（大多数站点对之间无直接传播关系）：

$$\mathcal{L}_{sparse} = \frac{1}{N^2} \sum_{i,j} \mathbf{1}[A_{ij}^{(t)} > 0]$$

目标：最小化非零边数量。

### 6.2 物理一致性约束

时延应与物理距离和风速一致：

$$\mathcal{L}_{physics} = \frac{1}{|\mathcal{E}|} \sum_{(i,j) \in \mathcal{E}} \left(\tau_{ij}^{(t)} - \frac{d_{ij}}{v_{wind}^{(t)} \cdot \max(\phi_{ij}^{(t)}, 0) + \epsilon}\right)^2$$

确保学习到的时延不会偏离物理计算值太远。

### 6.3 连通性约束

确保图不产生孤立节点（每个站点至少有入边或出边）：

$$\mathcal{L}_{conn} = \sum_{i=1}^{N} \mathbf{1}\left[\sum_{j} A_{ij}^{(t)} + \sum_{j} A_{ji}^{(t)} = 0\right]$$

---

## 七、算法伪代码

```
算法: DADGC — 时延感知动态图构建算法
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
输入: X ∈ R^{N×T×F} (多站点观测), W ∈ R^{T×M} (气象特征)
输出: {G₁, G₂, ..., G_T} (动态时延图序列)

// Phase 1: 静态时延发现
1:  FOR each pair (i, j), i ≠ j:
2:    FOR τ = 0 to τ_max:
3:      ρ_{ij}(τ) ← NCCF(x_i, x_j, τ)       // 归一化互相关
4:    END FOR
5:    τ̂_{ij} ← argmax_τ ρ_{ij}(τ)             // 最佳时延
6:    R_{ij} ← ρ_{ij}(τ̂_{ij})                  // 最大相关强度
7:  END FOR

// Phase 2: 因果筛选
8:  A^{causal} ← 0                             // 初始化因果邻接矩阵
9:  FOR each pair (i,j) where R_{ij} > θ_corr:
10:   IF GrangerTest(x_i → x_j, significance=0.05) THEN
11:     A^{causal}_{ij} ← 1
12:     T^{causal}_{ij} ← τ̂_{ij}
13:   END IF
14: END FOR

// Phase 3: 动态图演化 (每个时间步)
15: FOR t = 1 to T:
16:   θ_wind ← WindDirection(W_t)              // 当前风向
17:   v_wind ← WindSpeed(W_t)                  // 当前风速
18:   
19:   FOR each causal edge (i,j):
20:     θ_{ij} ← Azimuth(i → j)                // 站点方位角
21:     φ_{ij}^{(t)} ← cos(θ_wind - θ_{ij})    // 风向对齐因子
22:     τ_{ij}^{(t)} ← d_{ij} / (v_wind · max(φ_{ij}^{(t)}, 0) + ε)  // 动态时延
23:     
24:     // 动态边权 = 因果基础 × 风向调制 × 风速调制 × 时延衰减
25:     A_{ij}^{(t)} ← R_{ij}^{causal} · σ(φ_{ij}^{(t)}) · σ(v_wind/v_0) · exp(-τ_{ij}^{(t)}/τ_0)
26:   END FOR
27:   
28:   // 正则化：移除弱边
29:   A_{ij}^{(t)} ← 0 if A_{ij}^{(t)} < θ_edge
30:   
31:   G_t ← (V, E_t, A^{(t)}, τ^{(t)})        // 构建时延图
32: END FOR

33: RETURN {G_1, G_2, ..., G_T}
```

---

## 八、复杂度分析

| 步骤 | 时间复杂度 | 说明 |
|------|-----------|------|
| Step 1 互相关 | $O(N^2 \cdot T \cdot \tau_{max})$ | 所有站点对、所有时延 |
| Step 2 Granger检验 | $O(\|E_{candidate}\| \cdot T \cdot p)$ | 仅对候选边做检验 |
| Step 3 动态演化 | $O(\|E_{causal}\| \cdot T)$ | 每时间步更新因果边 |
| Step 4 正则化 | $O(\|E_{causal}\| \cdot T)$ | 阈值剪枝 |

**实际可行性**：EPA数据典型规模 $N \approx 50\text{-}200$，$\tau_{max} \approx 6$（小时），Step 1 和 Step 2 为离线预计算，Step 3 每时间步只需 $O(\|E_{causal}\|)$ 次简单运算，可实时执行。

---

## 九、创新点总结

### 与现有方法的本质区别

| 维度 | MTGNN/Graph WaveNet | Granger因果图 | DCRNN扩散卷积 | **本文DADGC** |
|------|---------------------|--------------|--------------|-------------|
| 图结构 | 静态自适应学习 | 静态因果图 | 静态有向图 | **动态时延图** |
| 时延建模 | ❌ 无 | 边权隐含 | 扩散步长近似 | ✅ **显式边属性** |
| 气象感知 | ❌ 无 | ❌ 无 | ❌ 无 | ✅ **风场调制** |
| 消息传递 | 同时间步 | 同时间步 | 多步扩散 | **跨时间步延迟** |
| 物理可解释 | 黑箱 | 因果方向 | 扩散过程 | **传播时延** |

### 三个层次的创新

1. **表示层创新**：首次在时空图中将时延 $\tau_{ij}$ 作为边的**一等公民属性**（与权重并列），而非隐含在参数中
2. **演化层创新**：图结构不是"学习一个静态最优图"，而是**受气象条件驱动的物理演化过程**，有明确的动力学方程
3. **因果层创新**：通过Granger因果筛选 + 互相关时延估计的**两阶段方法**，保证图边既有统计显著性又有因果方向性

---

# 第四章：基于时延动态图的时空图神经网络预测模型

## 一、模型整体架构

```
                  ┌─────────────────────────────────────────────┐
                  │           DADGC 动态时延图构建 (第三章)       │
                  │     G_t = (V, E_t, A^(t), τ^(t))            │
                  └──────────────────┬──────────────────────────┘
                                     │
                                     ▼
┌──────────┐  ┌──────────┐  ┌───────────────┐  ┌──────────┐
│ 节点特征  │  │ 时延边权重│  │ 延迟历史状态   │  │ 气象编码  │
│ X^(t)    │  │ A^(t)    │  │ {H^(t-τ_max)} │  │ Z_w^(t)  │
└────┬─────┘  └────┬─────┘  └──────┬────────┘  └────┬─────┘
     │              │               │                 │
     └──────────────┴───────┬───────┴─────────────────┘
                             │
                             ▼
              ┌──────────────────────────────┐
              │   时延感知空间卷积模块 (DASConv)   │
              │   H^(t) = DASConv(X^(t), A^(t),  │
              │                    τ^(t), H^(history))│
              └──────────────┬───────────────┘
                             │
                             ▼
              ┌──────────────────────────────┐
              │   多尺度时间卷积模块 (MSTConv)    │
              │   Y_spatial = MSTConv(H^(t))   │
              └──────────────┬───────────────┘
                             │
                             ▼
              ┌──────────────────────────────┐
              │   时空交叉门控融合 (STGFusion)   │
              │   Ŷ = Gate(Spatial, Temporal)   │
              └──────────────┬───────────────┘
                             │
                             ▼
                      预测输出 Ŷ
```

---

## 二、核心模块：时延感知空间卷积 (DASConv)

### 2.1 标准图卷积的问题

标准GCN消息传递：
$$h_i^{(t)} = \sigma\left(\sum_{j \in \mathcal{N}(i)} A_{ij} \cdot W \cdot h_j^{(t)}\right)$$

问题：所有邻居使用**同一时刻**的隐藏状态 $h_j^{(t)}$，忽略了传播时延。

### 2.2 时延感知消息传递

**核心修改**：邻居 $j$ 发送给节点 $i$ 的消息，不是 $h_j^{(t)}$，而是 $h_j^{(t - \tau_{ij}^{(t)})}$：

$$h_i^{(t)} = \sigma\left(\sum_{j \in \mathcal{N}(i)} A_{ij}^{(t)} \cdot W \cdot h_j^{(t - \tau_{ij}^{(t)})}\right)$$

**物理含义**：站点 $i$ 在时刻 $t$ 的状态，受其邻居 $j$ 在 $\tau_{ij}^{(t)}$ 时刻之前的状态影响，正好对应污染从 $j$ 传播到 $i$ 所需的时间。

### 2.3 分数时延的插值处理

$\tau_{ij}^{(t)}$ 可能是非整数（如 2.3 小时），需要插值：

$$h_j^{(t-\tau)} = \alpha \cdot h_j^{(t-\lfloor\tau\rfloor)} + (1-\alpha) \cdot h_j^{(t-\lceil\tau\rceil)}$$

其中 $\alpha = \lceil\tau\rceil - \tau$。

### 2.4 多头时延注意力扩展

借鉴GAT，将时延融入注意力机制：

$$\alpha_{ij}^{(t)} = \frac{\exp\left(\text{LeakyReLU}\left(\mathbf{a}^T [W h_i^{(t)} \| W h_j^{(t-\tau_{ij}^{(t)})} \| \mathbf{e}_{\tau_{ij}^{(t)}}]\right)\right)}{\sum_{k \in \mathcal{N}(i)} \exp\left(\text{LeakyReLU}\left(\mathbf{a}^T [W h_i^{(t)} \| W h_k^{(t-\tau_{ik}^{(t)})} \| \mathbf{e}_{\tau_{ik}^{(t)}}]\right)\right)}$$

其中 $\mathbf{e}_{\tau}$ 是**时延嵌入向量**（可学习），将离散/连续时延映射到高维空间：

$$\mathbf{e}_{\tau} = \text{Embed}(\lfloor\tau\rfloor) + (\tau - \lfloor\tau\rfloor) \cdot (\text{Embed}(\lceil\tau\rceil) - \text{Embed}(\lfloor\tau\rfloor))$$

多头扩展：
$$h_i^{(t)} = \|_{k=1}^{K} \sigma\left(\sum_{j \in \mathcal{N}(i)} \alpha_{ij}^{(t,k)} \cdot W^{(k)} \cdot h_j^{(t - \tau_{ij}^{(t)})}\right)$$

### 2.5 计算实现

时延消息传递需要**缓存历史隐藏状态**：

```python
# 伪代码
history_buffer = deque(maxlen=tau_max + 1)  # 存储最近tau_max步的隐藏状态

def delay_aware_message_passing(H_current, A_t, tau_t, history_buffer):
    H_delayed = torch.zeros_like(H_current)
    for i in range(N):
        for j in neighbors(i):
            tau = tau_t[i, j].int()
            frac = tau_t[i, j] - tau
            # 从历史缓存中检索延迟状态
            h_j_delayed = frac * history_buffer[tau][j] + (1-frac) * history_buffer[tau+1][j]
            H_delayed[i] += A_t[i,j] * h_j_delayed
    return H_delayed
```

---

## 三、多尺度时间卷积模块 (MSTConv)

### 3.1 设计动机

空气质量变化包含多尺度时间模式：
- **小时级**：早晚高峰排放
- **日级**：昼夜温差导致的扩散条件变化
- **周级**：工作日/周末排放差异

### 3.2 膨胀因果卷积

使用不同膨胀率捕捉不同尺度：

$$\text{MSTConv}(H) = \|_{d \in \{1, 2, 4, 8\}} \text{DilatedCausalConv}_d(H)$$

每个膨胀率 $d$ 的感受野为 $d \times \text{kernel\_size}$，覆盖不同时间尺度。

### 3.3 与时延的空间交互

时间卷积的输出会与时延空间卷积的输出在融合模块中交互（见下节）。

---

## 四、时空交叉门控融合 (STGFusion)

### 4.1 问题

空间特征（来自DASConv）和时间特征（来自MSTConv）包含不同维度的信息，需要自适应融合。

### 4.2 门控机制

$$\mathbf{g}^{(t)} = \sigma\left(W_g [\mathbf{H}_{spatial}^{(t)} \| \mathbf{H}_{temporal}^{(t)}] + b_g\right)$$

$$\mathbf{Y}^{(t)} = \mathbf{g}^{(t)} \odot \mathbf{H}_{spatial}^{(t)} + (1 - \mathbf{g}^{(t)}) \odot \mathbf{H}_{temporal}^{(t)}$$

门控 $\mathbf{g}$ 学习"在这个时间步、这个节点上，空间信息和时间信息哪个更重要"。

### 4.3 气象条件对门控的影响

门控还接收气象编码作为额外输入：

$$\mathbf{g}^{(t)} = \sigma\left(W_g [\mathbf{H}_{spatial}^{(t)} \| \mathbf{H}_{temporal}^{(t)} \| \mathbf{Z}_w^{(t)}] + b_g\right)$$

**物理含义**：大风天→空间传播强→门控偏向空间特征；静稳天→时间自相关强→门控偏向时间特征。

---

## 五、训练策略

### 5.1 总损失函数

$$\mathcal{L} = \underbrace{\mathcal{L}_{pred}}_{\text{预测损失}} + \lambda_1 \underbrace{\mathcal{L}_{sparse}}_{\text{稀疏正则}} + \lambda_2 \underbrace{\mathcal{L}_{physics}}_{\text{物理一致性}}$$

其中预测损失：
$$\mathcal{L}_{pred} = \frac{1}{N \cdot T_{horizon}} \sum_{i=1}^{N} \sum_{t=1}^{T_{horizon}} \|\hat{x}_i^{(t)} - x_i^{(t)}\|^2$$

### 5.2 两阶段训练

**阶段一（离线）**：运行DADGC算法，预计算动态时延图序列
- 互相关 → Granger因果 → 静态因果时延图
- 此阶段**不参与反向传播**，结果是确定性的

**阶段二（端到端）**：固定图结构，训练预测模型
- 加载预计算的 $\{A^{(t)}, \tau^{(t)}\}$
- 训练DASConv + MSTConv + STGFusion
- 时延嵌入 $\mathbf{e}_\tau$ 和缩放参数 $v_0, \tau_0$ 可微调

### 5.3 可选扩展：图结构微调

如果计算资源允许，可以将Step 3的气象调制参数纳入端到端训练：

$$A_{ij}^{(t)} = R_{ij}^{causal} \cdot \sigma(\phi_{ij}^{(t)} + b_\phi) \cdot \sigma\left(\frac{v_{wind}^{(t)}}{v_0}\right) \cdot \exp\left(-\frac{\tau_{ij}^{(t)}}{\tau_0}\right)$$

其中 $b_\phi, v_0, \tau_0$ 为可学习参数。

---

## 六、模型可解释性

### 6.1 图结构可解释

- 每条边有**明确的物理含义**：$A_{ij}^{(t)}$ 大 → 站点 $j$ 在当前风场下对站点 $i$ 有强影响
- 时延 $\tau_{ij}^{(t)}$ 直接对应**传播时间**
- 风向对齐因子 $\phi_{ij}^{(t)}$ 可视化 → 看到污染传播路径

### 6.2 注意力权重可解释

多头时延注意力中的 $\alpha_{ij}^{(t,k)}$ 可视化：
- 哪些站点是当前时刻的关键影响源
- 不同注意力头关注不同的时延尺度

### 6.3 门控权重可解释

时空门控 $\mathbf{g}^{(t)}$ 可视化：
- 大风天 vs 静稳天的门控分布差异
- 不同站点的时空依赖模式

---

## 七、预期实验设计

### 7.1 数据集

- **主数据集**：美国EPA Air Quality System (AQS)
  - 选择一个州或区域（如加州、纽约州）
  - PM2.5小时浓度 + 气象数据（风向、风速、温度、湿度）
  - 时间跨度：≥2年

### 7.2 基线方法

| 类别 | 方法 | 代表 |
|------|------|------|
| 传统 | HA, ARIMA | 统计方法 |
| 深度时序 | LSTM, GRU | 无空间建模 |
| 时空CNN | ConvLSTM | 网格化空间 |
| 静态STGNN | STGCN, DCRNN | 固定图 |
| 自适应STGNN | Graph WaveNet, MTGNN, AGCRN | 学习图 |
| 注意力STGNN | ASTGCN, STGNN+Transformer | 注意力权重 |
| 最新对比 | PDFormer, STGODE | Transformer/ODE |

### 7.3 消融实验

| 实验编号 | 移除组件 | 验证什么 |
|----------|---------|---------|
| A1 | 去掉时延（τ=0） | 时延建模的贡献 |
| A2 | 去掉气象调制（静态图） | 动态图的贡献 |
| A3 | 去掉Granger筛选（用互相关直接建图） | 因果筛选的贡献 |
| A4 | 去掉多头注意力（单头DASConv） | 多头注意力的贡献 |
| A5 | 去掉门控融合（直接拼接） | 门控融合的贡献 |

### 7.4 可视化分析

- 不同风向下动态图结构的可视化对比
- 时延矩阵热力图
- 注意力权重在典型污染事件中的变化
- 门控权重与气象条件的关联

---

## 八、论文章节结构建议

### 第三章：时延感知动态图构建算法

```
3.1 问题定义与形式化
    3.1.1 大气污染传播的时延特性分析
    3.1.2 动态时延图的形式化定义
    
3.2 基于互相关的时延发现
    3.2.1 归一化互相关时延估计
    3.2.2 时延矩阵的构建与性质
    
3.3 基于Granger因果的边筛选
    3.3.1 条件Granger因果检验
    3.3.2 因果时延图的构建
    
3.4 气象驱动的动态图演化
    3.4.1 风向对齐因子
    3.4.2 动态时延计算
    3.4.3 动态边权计算
    
3.5 图正则化与物理约束
    3.5.1 稀疏性约束
    3.5.2 物理一致性约束
    3.5.3 连通性保证
    
3.6 算法复杂度分析
3.7 动态图可视化与实例分析
```

### 第四章：基于时延动态图的空气质量预测模型

```
4.1 模型总体架构
4.2 时延感知空间卷积 (DASConv)
    4.2.1 时延感知消息传递机制
    4.2.2 分数时延插值
    4.2.3 多头时延注意力
4.3 多尺度时间卷积 (MSTConv)
    4.3.1 膨胀因果卷积
    4.3.2 多尺度感受野设计
4.4 时空交叉门控融合 (STGFusion)
    4.4.1 门控机制设计
    4.4.2 气象条件对门控的影响
4.5 模型训练策略
    4.5.1 两阶段训练
    4.5.2 损失函数设计
4.6 模型可解释性分析
```
