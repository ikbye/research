# 第3章 时延感知动态图构建算法

## 3.1 引言

时空图神经网络（Spatio-Temporal Graph Neural Network, STGNN）在空气质量预测中取得了显著进展，但现有方法在处理站点间传播时延时存在结构性缺陷。本章提出一种时延感知动态图构建算法（Delay-Aware Dynamic Graph Construction, DADGC），从物理传播机制出发，构建显式编码传播时延的动态有向图结构，为后续第4章的时延感知STGNN预测模型提供图基础。

### 3.1.1 现有方法的局限性

现有STGNN对站点间关联的建模可归纳为三种范式，每种范式对传播时延的处理均存在根本性不足。

范式一：预定义静态图。基于地理距离或KNN构建固定的邻接矩阵 $\mathbf{A}$，并在所有时间步上共享使用。该假设认为站点间的依赖关系不随时间变化且无传播延迟，与大气污染随气象条件动态传播的物理事实严重不符。

范式二：自适应图学习。如GraphWaveNet、MTGNN等通过学习节点嵌入的内积 $\mathbf{E}_1\mathbf{E}_2^\top$ 生成邻接矩阵。该类方法虽然让图结构可学习，但学到的仍是静态拓扑 $\mathbf{A}_{\text{static}}$，其核心假设是存在一个全局最优图刻画所有时间步的站点关系。此外，边权重仅表示"关联强度"，不携带任何时延信息。

范式三：注意力加权图。如GAT、ASTGCN等通过注意力机制为边动态分配权重 $\alpha_{ij}^{(t)}$。注意力权重虽然随时间步变化，但其反映的是节点特征的即时相似度，而非污染传播的物理时延。注意力权重 $W_{ij}^{(t)}$ 大并不等价于站点 $i$ 对站点 $j$ 有 $\tau$ 步的因果影响。

### 3.1.2 核心思路：从"关联强度"到"传播时延"

三类范式的共同缺陷在于：仅建模了"邻居之间是否有关系"（边的存在性）和"关系有多强"（边的权重），而忽略了"关系需要多久传递"（边的时延）。

本章的核心思想是：将图从传统的单层表示 $\mathbf{A} \in \mathbb{R}^{N \times N}$ 扩展为双层表示：

$$\text{传统表示：} \quad \mathbf{A} \quad \text{（仅边权重）}$$

$$\text{本文表示：} \quad \langle \mathbf{A}^{(t)}, \boldsymbol{\tau}^{(t)} \rangle \quad \text{（边权重 + 边时延）}$$

其中 $\mathbf{A}^{(t)}$ 编码站点间的影响强度，$\boldsymbol{\tau}^{(t)}$ 编码影响从源站点传播到目标站点所需的时间步数。这一双层表示构成了后续时延感知消息传递（第4.2节）的图基础。

### 3.1.3 算法总体框架

DADGC算法包含四个递进阶段：

1. 时延发现（3.3节）：通过归一化互相关函数对每对站点进行时延估计，获得初始时延矩阵 $\mathbf{T}$
2. 因果筛选（3.4节）：通过条件Granger因果检验过滤互相关中可能存在的伪关联，保留统计上显著的因果边
3. 动态演化（3.5节）：以气象特征为条件变量，驱动图结构和时延随环境动态变化
4. 图正则化（3.6节）：施加稀疏性与物理一致性约束，保证图结构的质量

## 3.2 问题形式化

### 3.2.1 基本定义

**定义3.1（多站点监测系统）** 设 $N$ 个空气质量监测站点构成集合 $\mathcal{V} = \{v_1, v_2, \ldots, v_N\}$，每个站点 $v_i$ 有地理坐标 $(lng_i, lat_i)$。在离散时间 $t = 1, 2, \ldots, T$ 上，观测记作 $\mathbf{X}^{(t)} \in \mathbb{R}^{N \times F}$，其中 $F$ 为污染物浓度及气象要素的特征维度。气象状态序列记作 $\mathbf{W} = \{\mathbf{w}^{(1)}, \mathbf{w}^{(2)}, \ldots, \mathbf{w}^{(T)}\}$。

**定义3.2（双层动态图）** 时延感知动态图定义为三元组 $\mathcal{G}_t = (\mathcal{V}, \mathbf{A}^{(t)}, \boldsymbol{\tau}^{(t)})$，其中：

- $\mathbf{A}^{(t)} \in \mathbb{R}^{N \times N}$ 为 $t$ 时刻的有向加权邻接矩阵，$A_{ij}^{(t)}$ 表示站点 $v_j$ 对站点 $v_i$ 在 $t$ 时刻的影响强度，越大越强；
- $\boldsymbol{\tau}^{(t)} \in \mathbb{R}^{N \times N}$ 为 $t$ 时刻的时延矩阵，$\tau_{ij}^{(t)} \in [0, \tau_{\max}]$ 表示污染从站点 $v_i$ 传播到站点 $v_j$ 所需的时间步数。

注：下标 ${ij}$ 约定为"从 $v_i$ 到 $v_j$"的传播方向。$A_{ij}^{(t)}$ 和 $\tau_{ij}^{(t)}$ 成对出现、共同编码一条有向边。

**定义3.3（时延感知邻域）** 站点 $v_j$ 在时延 $\tau_{ij}^{(t)}$ 下构成站点 $v_i$ 在 $t$ 时刻的时延感知邻域：

$$\mathcal{N}^{(t)}(i) = \left\{ (j, \tau_{ij}^{(t)}) \mid A_{ij}^{(t)} > \theta_{\text{edge}} \right\}$$

其中 $\theta_{\text{edge}}$ 为边存在性阈值。

### 3.2.2 与经典图表示的关键区别

为便于理解，表3.1对比了DADGC双层图表示与经典图表示的根本区别。

**表3.1 DADGC双层图与经典图表示的关键区别**

| 维度 | 经典图表示 | DADGC双层图表示 |
|------|-----------|----------------|
| 边属性 | 权重 $w_{ij}$（影响强度） | 权重 $A_{ij}^{(t)}$ + 时延 $\tau_{ij}^{(t)}$（影响强度 + 传播时间） |
| 图动态性 | 静态或注意力动态 | 气象条件驱动的物理动态 |
| 方向性 | 无向或对称 | 严格有向（顺风强、逆风弱） |
| 消息传递 | 同时间步聚合 $h_j^{(t)}$ | 跨时间步聚合 $h_j^{(t-\tau_{ij}^{(t)})}$ |
| 物理可解释 | 黑箱关联 | 时延对应传播速度，方向对应风向 |

### 3.2.3 本章目标

给定历史观测序列 $\mathbf{X}^{(1:T)}$ 和气象序列 $\mathbf{W}^{(1:T)}$，本章的目标是构建每个时间步的双层动态图序列 $\{\mathcal{G}_1, \mathcal{G}_2, \ldots, \mathcal{G}_T\}$，使得：

$$\min_{\mathbf{A}^{(t)}, \boldsymbol{\tau}^{(t)}} \mathcal{L}_{\text{pred}} + \lambda_1 \mathcal{L}_{\text{sparse}} + \lambda_2 \mathcal{L}_{\text{physics}}$$

其中 $\mathcal{L}_{\text{pred}}$ 为使用该图进行预测任务的损失（定义见第4.5节），$\mathcal{L}_{\text{sparse}}$ 为图稀疏性正则项，$\mathcal{L}_{\text{physics}}$ 为物理一致性正则项。

## 3.3 基于互相关的传播时延初估计

### 3.3.1 归一化互相关函数

对于站点 $v_i$ 和 $v_j$ 的浓度观测序列 $x_i, x_j \in \mathbb{R}^T$，定义归一化互相关函数（Normalized Cross-Correlation Function, NCCF）为：

$$\rho_{ij}(\tau) = \frac{\displaystyle\sum_{t=\tau+1}^{T} (x_i^{(t-\tau)} - \bar{x}_i)(x_j^{(t)} - \bar{x}_j)}{\sqrt{\displaystyle\sum_{t=1}^{T}(x_i^{(t)}-\bar{x}_i)^2} \cdot \sqrt{\displaystyle\sum_{t=1}^{T}(x_j^{(t)}-\bar{x}_j)^2}}, \quad \tau = 0, 1, \ldots, \tau_{\max}$$

其中 $\bar{x}_i$ 和 $\bar{x}_j$ 为序列均值，$\tau_{\max}$ 为物理上合理的最大传播时延（根据EPA气象经验，通常取6小时）。

物理含义：$\rho_{ij}(\tau)$ 度量的是"$v_i$ 在 $\tau$ 步前的观测与 $v_j$ 的当前观测之间的线性相关性"。若 $\rho_{ij}(\tau)$ 在某个 $\tau > 0$ 处取得显著峰值，则表明 $v_i$ 的变化领先于 $v_j$ 约 $\tau$ 个时间步，可能存在从 $v_i$ 到 $v_j$ 的污染传播路径。

### 3.3.2 时延估计

在相关性存在显著峰值的前提下，初始时延估计为：

$$\hat{\tau}_{ij}^{(0)} = \arg\max_{\tau \in [1, \tau_{\max}]} \rho_{ij}(\tau)$$

相应的峰值相关系数为：

$$R_{ij} = \rho_{ij}(\hat{\tau}_{ij}^{(0)})$$

### 3.3.3 距离约束与物理合理性

并非所有互相关峰值都对应真实的物理传播。引入距离约束进行初筛：若 $R_{ij} > \theta_{\text{corr}}$ 且 $d_{ij} \leq d_{\max}$，则保留时延估计 $\tilde{\tau}_{ij} = \hat{\tau}_{ij}^{(0)}$；否则置 $\tilde{\tau}_{ij} = \infty$。其中 $d_{ij}$ 为站点间欧氏距离。该约束确保时延估计满足以下物理前提：两个站点必须地理上相邻（$d_{ij} \leq d_{\max}$），远距离站点间的高相关性可能来自共同的气象背景；相关性必须统计显著（$R_{ij} > \theta_{\text{corr}}$），避免噪声干扰。

### 3.3.4 单向性分析

**定理3.1（时延单向性）** 若 $\hat{\tau}_{ij}^{(0)} = \tau^* > 0$，则 $\hat{\tau}_{ji}^{(0)}$ 必然满足 $\hat{\tau}_{ji}^{(0)} \neq \tau^*$，即传播时延具有单向性。

**证明** 由NCCF定义，$\rho_{ij}(\tau)$ 衡量的是 $x_i^{(t-\tau)}$ 与 $x_j^{(t)}$ 的相关性，而 $\rho_{ji}(\tau)$ 衡量的是 $x_j^{(t-\tau)}$ 与 $x_i^{(t)}$ 的相关性。若 $\rho_{ij}(\tau^*)$ 取最大，说明 $x_i$ 领先 $x_j$ 约 $\tau^*$ 步，相应地 $\rho_{ji}(\tau^*)$ 应较小（除非序列完全对称）。因此在一般情况下 $\hat{\tau}_{ij}^{(0)} \neq \hat{\tau}_{ji}^{(0)}$。

推论：时延矩阵 $\boldsymbol{\tau}^{(0)}$ 非对称，诱导的邻接矩阵 $\mathbf{A}^{(0)}$ 亦非对称。这为构建有向图提供了理论依据。

### 3.3.5 输出

此阶段输出两个矩阵：初始时延矩阵 $\mathbf{T}^{(0)} = [\tilde{\tau}_{ij}] \in \mathbb{R}^{N \times N}$（$\infty$ 表示无边）和峰值相关矩阵 $\mathbf{R} = [R_{ij}] \in \mathbb{R}^{N \times N}$。

## 3.4 基于Granger因果的伪关联过滤

### 3.4.1 问题背景

互相关虽能发现时间上的领先-滞后关系，但无法区分"直接因果"和"伪关联"。考虑如下典型场景：站点 $v_A$、$v_B$ 均位于大型污染源 $v_C$ 的下风向，$v_A$ 和 $v_B$ 的浓度序列高度相关且峰值时延 $\tau_{AB} > 0$，但 $v_A \to v_B$ 的传播是假的——二者均受 $v_C$ 影响，彼此无直接因果联系。为解决此问题，引入条件Granger因果检验对候选边进行过滤。

### 3.4.2 条件Granger因果检验

**定义3.4（条件Granger因果）** 给定多元时间序列 $\{v_1, v_2, \ldots, v_N\}$、设定最大滞后阶数 $p$，称 $v_i$ 在给定其全部 $N-1$ 个过去信息后仍然Granger导致 $v_j$，当且仅当加入 $v_i$ 的 $p$ 阶滞后项可以显著提升对 $v_j$ 的预测能力。

构建两个自回归模型。

受限模型（排除了 $v_i$ 的滞后信息）：

$$x_j^{(t)} = \alpha_0 + \sum_{k \in \mathcal{S}_{ij}} \sum_{l=1}^{p} \alpha_{kl} x_k^{(t-l)} + \varepsilon_R^{(t)}$$

其中 $\mathcal{S}_{ij} = \{m \mid \tilde{\tau}_{mj} \neq \infty, m \neq i\}$ 为所有通过时延初筛且不等于 $i$ 的站点。

完整模型（包含 $v_i$ 的滞后信息）：

$$x_j^{(t)} = \beta_0 + \sum_{k \in \mathcal{S}_{ij} \cup \{i\}} \sum_{l=1}^{p} \beta_{kl} x_k^{(t-l)} + \varepsilon_U^{(t)}$$

检验统计量为：

$$F = \frac{(\text{RSS}_R - \text{RSS}_U) / p}{\text{RSS}_U / (T - |\mathcal{S}_{ij} \cup \{i\}| \cdot p - 1)} \sim F(p, T - |\mathcal{S}_{ij} \cup \{i\}| \cdot p - 1)$$

其中 $\text{RSS}_R$ 和 $\text{RSS}_U$ 分别为受限模型和完整模型的残差平方和。

### 3.4.3 时延约束的 $\tau$-Granger检验

标准Granger因果检验的滞后阶数为 $p$，无特定的时延偏好。为与前面的时延估计结合，本文提出 $\tau$-Granger检验。

**定义3.5（$\tau$-Granger因果）** 称 $v_i$ 在时延 $\tau$ 下 $\tau$-Granger导致 $v_j$，若在完整模型中 $\beta_{i\tau} \neq 0$ 在显著性水平0.05下显著，即时延 $\tau$ 处的系数显著异于零。

$$\text{因果边集} = \left\{(i \to j) \mid \tilde{\tau}_{ij} \neq \infty, \; \beta_{i\tilde{\tau}_{ij}} \text{ 在 } p < 0.05 \text{ 下显著} \right\}$$

### 3.4.4 输出

输出因果邻接矩阵 $\mathbf{A}^{\text{causal}} \in \{0, 1\}^{N \times N}$（因果边为1）和因果时延矩阵 $\mathbf{T}^{\text{causal}} = \mathbf{T}^{(0)} \odot \mathbf{A}^{\text{causal}}$（其中 $\odot$ 为逐元素乘），仅在Granger因果成立的边上保留时延值，其余位置置 $\infty$。

## 3.5 气象驱动的图结构动态演化

本节是DADGC算法的核心创新。前两节（3.3、3.4）构建了一个静态的因果时延图——它反映了"在整体统计上，污染在哪些站点对之间有显著的传播关系"。然而，大气污染的物理传播具有时变性：同样的站点对，在顺风条件下传播强、时延短；在逆风条件下传播弱甚至无传播。本节提出一种气象驱动的动态图演化机制，将静态因果图转化为随时间步动态演化的双层图。

### 3.5.1 风向对齐因子

**定义3.6（风向对齐因子）** 设 $t$ 时刻风场状态 $\mathbf{w}^{(t)}$ 中包含风向角 $\theta_{\text{wind}}^{(t)} \in [0, 2\pi)$ 和风速 $v_{\text{wind}}^{(t)} \geq 0$。站点 $v_i$ 到 $v_j$ 的地理方位角为 $\theta_{ij} \in [0, 2\pi)$。定义风向对齐因子：

$$\phi_{ij}^{(t)} = \max\left( \cos(\theta_{\text{wind}}^{(t)} - \theta_{ij}), \; 0 \right)$$

物理含义：

- $\phi_{ij}^{(t)} = \cos(0) = 1$：风向正好从 $v_i$ 指向 $v_j$，为完美顺风，传播最强
- $\phi_{ij}^{(t)} = \cos(\pi/2) = 0$：风向垂直于 $v_i \to v_j$ 方向，传播弱
- $\phi_{ij}^{(t)} = \max(\cos(\pi), 0) = 0$：风从 $v_j$ 吹向 $v_i$（逆风），几乎无传播

与现有方法的区别：MTGNN/GraphWaveNet的节点嵌入内积 $E_i^\top E_j$ 与物理无关；DCRNN的预定义邻接矩阵有向扩散无气象调制；本文对齐因子 $\phi_{ij}^{(t)}$ 具有显式的物理几何意义，图方向由实际气象条件决定。

### 3.5.2 动态时延计算

初始静态时延 $\tau_{ij}^{\text{causal}}$ 描述的是"统计平均传播时间"。在气象条件下，实际传播时间随风速和风向动态变化：

$$\tau_{ij}^{(t)} = \frac{d_{ij}}{v_{\text{wind}}^{(t)} \cdot \phi_{ij}^{(t)} + \varepsilon} \cdot \mathbb{1}[a_{ij}^{\text{causal}} = 1]$$

其中 $d_{ij}$ 为站点间地理距离，$\varepsilon = 10^{-6}$ 为防止除零。该式的物理直觉：

- 风速 $v_{\text{wind}}^{(t)}$ 大 → 分母大 → $\tau_{ij}^{(t)}$ 小（传播快，时延短）
- 顺风 $\phi_{ij}^{(t)} \approx 1$ → 分母大 → $\tau_{ij}^{(t)}$ 小（风速充分作用于传播）
- 逆风 $\phi_{ij}^{(t)} = 0$ → 分母为 $\varepsilon$ → $\tau_{ij}^{(t)}$ 极大 → 边权被后续衰减函数抑制

当 $\tau_{ij}^{(t)} > \tau_{\max}$ 时，该边在 $t$ 时刻被自动剪除。

### 3.5.3 动态边权计算

结合检测出的因果强度 $R_{ij}$ 和气象条件，动态边权为四个因子的连乘：

$$A_{ij}^{(t)} = \underbrace{R_{ij}}_{\substack{\text{因果} \\ \text{基础}}} \cdot \underbrace{\sigma(\phi_{ij}^{(t)})}_{\substack{\text{风向} \\ \text{调制}}} \cdot \underbrace{\sigma\left(\frac{v_{\text{wind}}^{(t)}}{v_0}\right)}_{\substack{\text{风速} \\ \text{调制}}} \cdot \underbrace{\exp\left(-\frac{\tau_{ij}^{(t)}}{\tau_0}\right)}_{\substack{\text{时延} \\ \text{衰减}}}$$

各因子的作用与设计动机如下：

(a) 因果基础 $R_{ij}$：来自互相关估计的峰值相关系数（3.3节），提供静态因果强度。这是时变调制的锚点，保证动态图不偏离数据驱动的统计事实。

(b) 风向调制 $\sigma(\phi_{ij}^{(t)})$：Sigmoid函数 $\sigma(x) = 1/(1+e^{-x})$ 将对齐因子映射到 $(0,1)$。顺风方向（$\phi \approx 1$）时 $\sigma(1) \approx 0.73$，保留大部分强度；逆风方向（$\phi = 0$）时 $\sigma(0) = 0.5$，强度折半；风场不相关时退化为0.5的基线。

(c) 风速调制 $\sigma(v_{\text{wind}}^{(t)}/v_0)$：$v_0$ 为可学习参数，控制风速敏感度。风速 $v \gg v_0$ 时 $\sigma(v/v_0) \to 1$，传播充分；风速 $v \ll v_0$ 时 $\sigma(v/v_0) \to 0.5$，传播受限。

(d) 时延衰减 $\exp(-\tau_{ij}^{(t)}/\tau_0)$：$\tau_0$ 为可学习参数，控制时延衰减率。$\tau_{ij}^{(t)} = 0$ 时衰减因子为1（即时影响）；$\tau_{ij}^{(t)} = \tau_0$ 时衰减因子为 $e^{-1} \approx 0.37$；$\tau_{ij}^{(t)} = 3\tau_0$ 时衰减因子为 $e^{-3} \approx 0.05$（近似忽略）。

注：参数 $v_0$ 和 $\tau_0$ 在后级端到端训练（第4.5节）中可作为可训练参数微调，从而实现"物理先验初始化 + 数据驱动精调"。

### 3.5.4 与端到端图学习的本质区别

DADGC的气象驱动演化与现有自适应图学习方法的本质区别见表3.2。

**表3.2 DADGC气象驱动演化与自适应图学习的对比**

| 维度 | 自适应图学习(MTGNN等) | DADGC气象驱动演化 |
|------|----------------------------------|-------------------|
| 图生成方式 | 节点嵌入 $E_1, E_2$ 通过内积生成加权邻接 | 物理公式 $f(R, \phi, v, \tau)$ 直接计算 |
| 参数含义 | 嵌入向量无物理意义 | 所有参数 $\phi, v, \tau, R$ 均有物理含义 |
| 可解释性 | 黑箱：无法解释为什么边强或弱 | 白箱：每个因子可单独可视化和分析 |
| 泛化性 | 训练集上统计模式容易过拟合 | 物理规律可迁移到新站点、新时间段 |
| 图更新频率 | 训练过程中缓慢更新 | 每个时间步根据气象快照即时更新 |

### 3.5.5 时变图的可微分处理

完整的动态图生成过程是完全可微的：

$$\mathbf{A}^{(t)} = \mathbf{R} \odot \sigma(\boldsymbol{\Phi}^{(t)}) \odot \sigma(\mathbf{v}_{\text{wind}}^{(t)} / v_0) \odot \exp(-\boldsymbol{\tau}^{(t)} / \tau_0)$$

其中 $\boldsymbol{\Phi}^{(t)} = [\phi_{ij}^{(t)}]$ 为风向对齐因子矩阵，$\boldsymbol{\tau}^{(t)} = [\tau_{ij}^{(t)}]$ 为动态时延矩阵。可微性保证了动态图参数（$v_0, \tau_0$）可以在后续预测任务的端到端训练中通过反向传播进行微调（参见第4.5节）。

### 3.5.6 输出

输出动态图序列 $\mathcal{G}_t = (\mathcal{V}, \mathbf{A}^{(t)}, \boldsymbol{\tau}^{(t)})$，$t = 1, 2, \ldots, T$，其中 $\mathbf{A}^{(t)}$ 和 $\boldsymbol{\tau}^{(t)}$ 的动态变化完全由气象序列 $\mathbf{W}^{(t)}$ 驱动。

## 3.6 图正则化与物理约束

动态构建的图序列 $\{\mathcal{G}_t\}$ 需满足以下约束确保质量。

### 3.6.1 稀疏性正则化

大气污染传播是稀疏的——一个站点通常只受少数上游站点的显著影响。对 $\mathbf{A}^{(t)}$ 施加 $\ell_0$ 松弛约束：

$$\mathcal{L}_{\text{sparse}} = \frac{1}{N^2 T} \sum_{t=1}^{T} \sum_{i=1}^{N} \sum_{j=1}^{N} \mathbb{1}[A_{ij}^{(t)} > \theta_{\text{edge}}]$$

训练时，$\theta_{\text{edge}}$ 设定为非零边比例上限（如5%对应 $\theta_{\text{edge}}$ 取top-5%分位数）。

### 3.6.2 物理一致性约束

动态时延 $\tau_{ij}^{(t)}$ 应与物理计算的时延一致：

$$\mathcal{L}_{\text{physics}} = \frac{1}{|\mathcal{E}_{\text{causal}}| T} \sum_{t=1}^{T} \sum_{(i,j) \in \mathcal{E}_{\text{causal}}} \left( \tau_{ij}^{(t)} - \frac{d_{ij}}{v_{\text{wind}}^{(t)} \cdot \phi_{ij}^{(t)} + \varepsilon} \right)^2$$

其中 $\mathcal{E}_{\text{causal}}$ 为所有因果边集合。该正则项防止端到端微调（第4.5节）中的时延过度偏离物理计算值。

### 3.6.3 连通性约束

每个站点至少应有一条入边或出边：

$$\mathcal{L}_{\text{conn}} = \frac{1}{T} \sum_{t=1}^{T} \sum_{i=1}^{N} \mathbb{1}\left[ \sum_{j} A_{ij}^{(t)} + \sum_{j} A_{ji}^{(t)} = 0 \right]$$

该约束保证没有孤立节点，确保覆盖全站点网络的传播关系。

### 3.6.4 总正则化损失

$$\mathcal{L}_{\text{graph}} = \lambda_1 \mathcal{L}_{\text{sparse}} + \lambda_2 \mathcal{L}_{\text{physics}} + \lambda_3 \mathcal{L}_{\text{conn}}$$

超参数经验设定：$\lambda_1 = 10^{-3}$，$\lambda_2 = 0.1$，$\lambda_3 = 1.0$。

## 3.7 算法整体流程与复杂度分析

### 3.7.1 算法伪代码

```
算法3.1 时延感知动态图构建算法 (DADGC)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
输入: X ∈ ℝ^{N×T×F} (多站点时序观测)
      W ∈ ℝ^{T×M} (气象特征, 含风向风速)
      τ_max, θ_corr, p_crit (超参数)
输出: 动态图序列 {G_1, ..., G_T}, G_t = (V, A^(t), τ^(t))
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// Phase I: 时延估计 (离线预计算)
1:  FOR 每对站点 (i, j), i ≠ j:
2:    τ̂_ij ← argmax_{τ∈[1,τ_max]} ρ_ij(τ)         // 式(3-5)
3:    R_ij ← ρ_ij(τ̂_ij)
4:    IF R_ij < θ_corr OR d_ij > d_max:
5:      τ̃_ij ← ∞                                    // 初筛不通过
6:    ELSE:
7:      τ̃_ij ← τ̂_ij
8:    END IF
9:  END FOR

// Phase II: Granger 因果筛选 (离线预计算)
10: A^{causal} ← 0                                    // 初始化因果邻接
11: FOR 每对 (i,j) where τ̃_ij ≠ ∞:
12:   构建受限模型与完整模型                           // 式(3-7)(3-8)
13:   计算 F 统计量及 p-value
14:   IF p-value < 0.05:
15:     a^{causal}_ij ← 1
16:   END IF
17: END FOR

// Phase III: 动态演化 (在线生成, 每时间步)
18: FOR t = 1 to T:
19:   θ_wind ← W[t, 风向列]                           // 当前风向
20:   v_wind ← W[t, 风速列]                           // 当前风速
21:   FOR 每个因果边 (i,j):
22:     θ_ij ← Azimuth(v_i → v_j)                    // 计算方位角
23:     φ ← max(cos(θ_wind - θ_ij), 0)               // 风向对齐因子
24:     τ ← d_ij / (v_wind · φ + ε)                  // 动态时延
25:     a ← R_ij · σ(φ) · σ(v_wind/v_0) · exp(-τ/τ_0) // 动态边权
26:     A[t, i, j] ← a;  τ[t, i, j] ← τ
27:   END FOR
28:   G_t ← (V, A[t], τ[t])
29:   IF 正则化使能: 应用 ℒ_sparse, ℒ_physics, ℒ_conn
30: END FOR

31: RETURN {G_1, G_2, ..., G_T}
```

### 3.7.2 复杂度分析

**表3.3 DADGC算法复杂度**

| 阶段 | 时间复杂度 | 空间复杂度 | 执行模式 |
|------|-----------|-----------|----------|
| Phase I: 时延估计 | $O(N^2 \cdot T \cdot \tau_{\max})$ | $O(N^2)$ | 离线预计算 |
| Phase II: Granger检验 | $O(|\mathcal{E}_{\text{cand}}| \cdot T \cdot p \cdot N)$ | $O(N^2)$ | 离线预计算 |
| Phase III: 动态演化 | $O(|\mathcal{E}_{\text{causal}}| \cdot T)$ | $O(N^2)$ | 在线生成 |

其中 $\tau_{\max} \approx 6$（小时），$|\mathcal{E}_{\text{causal}}| \ll N^2$（稀疏因果图）。以EPA数据典型规模（$N \approx 100$ 站点，$T \approx 8760$ 全年小时数据）为例：Phase I/II离线阶段约 $10^6$ 次互相关计算，现代CPU上约5-10分钟；Phase III在线阶段每时间步约 $10^4$ 次标量运算，可实时执行。

### 3.7.3 与现有方法的复杂度对比

**表3.4 与现有方法的复杂度对比**

| 方法 | 离线预计算 | 在线推理 | 可微调参数 |
|------|-----------|----------|-----------|
| 预定义KNN图 | $O(N^2 \log N)$ | $O(1)$ | 0 |
| MTGNN自适应图 | $O(N^2 d)$ | $O(N^2 d)$ | $2Nd$ |
| GraphWaveNet | $O(N^2 d)$ | $O(N^2 d)$ | $2Nd$ |
| GTS (强化学习) | $O(\text{大量})$ | $O(N^2 d)$ | $N^2$ |
| DADGC (本文) | $O(N^2 T \tau_{\max})$ | $O(|\mathcal{E}_{\text{causal}}|)$ | 2 |

关键优势：DADGC的在线推理复杂度与因果边数 $|\mathcal{E}_{\text{causal}}|$ 线性相关，而自适应图方法需要 $N^2 d$ 的矩阵乘法。在站点数 $N$ 较大时，DADGC效率优势显著。

## 3.8 与现有工作的本质区别

为便于审稿人和答辩委员会理解创新点，本节以定理形式总结DADGC与现有工作的根本性差异。

**定理3.2（表示能力差异）** 传统图表示为 $\mathbf{A} \in \mathbb{R}^{N \times N}$ 或 $\mathbf{A}^{(t)} \in \mathbb{R}^{N \times N}$，编码的是"谁与谁相关"及"相关强度"。DADGC表示为 $\langle \mathbf{A}^{(t)}, \boldsymbol{\tau}^{(t)} \rangle$，额外编码了"影响的到达时间"。$\boldsymbol{\tau}^{(t)}$ 包含传统表示无法表达的跨时间步关系信息。

**定理3.3（演化机制差异）**

- 自适应图（MTGNN等）的演化是由梯度下降驱动的设计，图参数在训练过程中缓慢更新
- 注意力图（GAT等）的演化是由特征相似度驱动的设计，边权重随输入特征变化
- DADGC的演化是由外部物理变量驱动的设计，图结构随气象条件即时变化，不需要更新任何参数即可响应环境变化

**定理3.4（信息传递差异）** 传统STGNN中，$h_i^{(t)}$ 的更新仅使用 $\{h_j^{(t)}\}$（同时间步）。DADGC通过 $\boldsymbol{\tau}^{(t)}$ 矩阵，使 $h_i^{(t)}$ 的更新能够使用 $\{h_j^{(t-\tau_{ij}^{(t)})}\}$（跨时间步）。这一机制打破了STGNN中"空间同步聚合"的根本假设。

## 3.9 本章小结

本章提出了时延感知动态图构建算法（DADGC），其核心贡献包括：

1. 双层图表示：将图从传统单层结构 $\mathbf{A}$ 扩展为 $\langle \mathbf{A}^{(t)}, \boldsymbol{\tau}^{(t)} \rangle$，在权重之外显式编码传播时延。这是对图神经网络中"边属性"概念的新扩展。

2. 两阶段时延发现：互相关时延估计 + $\tau$-Granger因果检验的组合，保证发现的时延边既有统计显著性又有因果方向性。$\tau$-Granger检验（定义3.5）是本文对标准Granger检验的针对性改造。

3. 气象驱动的动态演化机制（核心创新）：通过风向对齐因子（定义3.6）和四因子边权公式（式3-13），将物理驱动作为图演化的第一性原理，无需依赖训练数据即可即时响应气象变化。图结构和时延随风场的变化方式具有明确的物理几何意义。

4. 可微的物理先验参数：$v_0$ 和 $\tau_0$ 两个参数连接了物理模型与数据驱动模型，既保持了物理可解释性，又允许通过端到端训练进行场景自适应。

5. 极低的在线推理复杂度：$O(|\mathcal{E}_{\text{causal}}| \cdot T)$ 的推理复杂度远低于自适应图学习的 $O(N^2 d)$，适合大规模传感器网络。

第4章将基于DADGC构建的动态时延图，设计时延感知空间卷积（DASConv）作为STGNN的消息传递算子，从而在第3章和第4章之间形成"图构建 → 图应用"的完整闭环。